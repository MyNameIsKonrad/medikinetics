# Medikinetics — Architecture

Read on demand: before changing the PK model, rendering, storage, the service worker, or anything a Decision Log row names. `AGENTS.md` holds the rules every session needs; this file holds how the app works, how it got here, and why.

Line references are to `index.html` unless noted, as of 2026-09-22. They drift, so search by name.

## PK model

- One-compartment oral absorption (Bateman equation) in `phaseConc`. Elimination `KE = 0.347 /h` (t½ ≈ 2 h) is shared by every med.
- Each `MEDS` entry is `{ label, sublabel, totalMg, color, phases, fastedPhases?, shortLabel? }`. `phases` is a list of `{ offsetHours, mg, durationHours, ka }`, and a dose's curve is the sum of its phases. `totalMg` is set by hand, not derived from the phases; it drives "taken today", the clearing threshold, export mg and labels. `shortLabel` is what a log row shows when present. All phase iteration goes through `phasesFor(pill)`, which picks `fastedPhases` over `phases` when the type has them and the pill is not `fed`.
- `phaseConc` handles the equal-rate limit (`ka ≈ KE`) with its closed form instead of dividing by zero (PR #19).
- **Two phase kinds.** A bolus phase (`phaseConc`) releases its `mg` at once. A zero-order phase has `windowHours` and releases its `mg` at a constant rate over `[offsetHours, offsetHours + windowHours]`, each slice absorbed first-order at `ka`. `zeroOrderConc` is `phaseConc` integrated over the window: with `u = min(t, W)` and `I(c) = (e^(-c(t-u)) - e^(-c·t)) / c`, `C = NORM · mg/W · ka/(ka-KE) · (I(KE) - I(ka))`, plus its own equal-rate branch. A near-zero window reduces to the bolus. `phaseConcFor(ph, tH)` dispatches; both concentration paths (`concAtTime`, `buildCurve`) use it. On zero-order phases `durationHours` mirrors `windowHours` so the card shows the release window.
- **Concerta** (`concerta(mg)` factory → `CON72` / `CON36` / `CON18`): 22% bolus overcoat (display window 1h), then 20% over 1–4h, 36% over 4–7h, 22% over 7–12h, all ka 2.0. Fractions follow the FDA biopharmaceutics review's in-vivo cumulative release (≈20% at 2h, 30% at 4h, 55% at 6h, 70% at 8h, read off a plot) and label §11.1 (22% overcoat, rising release rate over 6–7h). Checked numerically at 72mg: peak 28.8 mg eq at 7.2h (label: Tmax 6.8 ± 1.8h, adults); the 1h value is 55% of peak (label Fig 1: ~57%; Markowitz 2003: 58%); AUC equals 72mg of IR (fractions sum to 1.0); the card crosses the 10%-of-totalMg clearing line at ≈14.9h. Dose-proportional at 18–144mg (label §12.3), so one shape scales. Sources: FDA label 2023 (accessdata.fda.gov, NDA 021121), FDA NDA 21-121 biopharmaceutics review (2000), Markowitz 2003 (PMID 12648029), Atenza SmPC (rejestry.ezdrowie.gov.pl, product 41244). (verified: node check of the shipped formulas, 2026-09-22)
- **"mg eq" is IR-peak-equivalent, not mg in the body.** `NORM` scales every phase so that 1 mg of IR (`ka = KA_REF = 2.0`) peaks at exactly 1.0; 10 mg IR peaks at 10.0 mg eq. The one shared `NORM` keeps every med on a single comparable scale. A slower `ka` gives a lower, later, wider peak for the same total exposure (area under the curve ≈ 41.6 per 10 mg at every `ka` tested, 0.2–2.0). That is correct pharmacokinetics for equal bioavailability, not a scaling bug. Per-`ka` normalization is ruled out: it would inflate slow meds' peaks and break comparison between meds. (verified: numeric check of `phaseConc` during the 2026-09-22 audit; answers issue #20, closed by the housekeeping PR)
- `durationHours` is a display parameter, not a PK constant. It drives phase status and progress bars (`pillCardBodyHTML`), card visibility (`pillIsVisible`) and the chart window's right edge (`getCurveWindow`). CR fasted uses `durationHours: 6`.

## Data and storage

- `localStorage['medikinetics-v1']` holds an array of `{ id, type, takenAt, fed? }`. `id` is `Date.now()` at log time (not `takenAt`); `fed` exists only on types that have `fastedPhases`.
- The full history is kept forever; every display and math path filters through `last24h()`. **`loadPills` drops any stored dose whose `type` is not a key in `MEDS`, and the next `savePills` writes the filtered array back.** Removing or renaming a `MEDS` key therefore erases that med's history. (verified: `loadPills`, line ~488)
- `exportData()` shares the full history as enriched JSON (mg, ISO time) through the Web Share API, falling back to a blob download. There is no import. The footer shows the total stored count, and the export button is hidden until a dose exists.
- Undo is single-level with an 8 s window (`UNDO_DURATION_MS`); a second delete replaces the pending undo, and restore re-sorts by `takenAt`.

## Rendering

- `render()` runs on load, on every mutation except the fed toggle (Decision #3), on resize (150 ms debounce), and on a 30 s interval. The interval is cleared while the page is hidden and restarted with an immediate render on return (`visibilitychange`).
- Dose buttons are built once from every non-`retired` `MEDS` entry, in declaration order, into a two-column grid (`#dose-grid`). With an odd count the first (main) button gets `.dose-btn--wide` and spans the row, so nothing is stranded; today that is Concerta 72 on top, 36 and 18 below. There is no debounce: after a real log every `.dose-btn` gets `disabled` + `.just-fired` (opacity .35) for 400 ms, and the `touchstart` press-feedback handler skips disabled elements, so feedback and action stay in sync.
- Pill cards carry `data-id="${pill.id}"`, which `toggleFed()` uses to find the card it updates in place. Log rows color their "clearing" status with `--clearing: #614f8a`; the card's own clearing line uses `--soft`.
- Dose time: offset chips `-30m`, `-1h`, `-2h`, `-3h`, `now` (default), plus an exact-time chip. A typed time later than now means yesterday at that time (`timestampForTimeInput`). After each real log the selection resets to `now`. `render()` skips rebuilding `#offset-row` while the time input is focused or `_timeInputActive` is set, so the native iOS picker survives the 30 s tick.
- Chart window (`getCurveWindow`): start = min(earliest dose since local midnight − 45 min, now − 60 min); end = max(last phase end of any 24 h dose + 4 h, now + 90 min). With no doses: now − 30 min to now + 8 h.
- Chart: 10-minute sampling; y-axis floor of 5 mg eq with 1.2× headroom; x-ticks every 1, 2 or 4 h by window length. `MEDS.IR.color` is the total-curve, gradient and tooltip-ring color.
- Two caches: `buildCurve` single-slot memo keyed on the pill fingerprint and window, and `_lastSvgKey`, which skips the SVG rebuild when the curve key, width and now-pixel are unchanged.
- Tooltip/scrub handlers attach once to `#chart-container` and read the shared `_chart` object. Scrubbing writes `scrubTime` and pre-fills the time chip but does not select it. The tooltip hides 2.5 s after touchend.
- Preview: a dose logged at a future time becomes one ephemeral preview in `simulatedPills` (never saved; replaces any earlier preview). It draws dashed, with a "preview · not logged" card and ×. The only way in is to activate the time chip and scrub the chart into the future (open issue #31).
- Cards stay visible while a phase is running or concentration is at least `CLEARING_THRESHOLD` (10%) of `totalMg`; after the last phase ends they show "clearing" with a time-left estimate from pure elimination. Progress bars drain from full to empty.
- The stats line: "taken today" counts since local midnight; "in system" sums the rolling 24 h; "↑ rising" compares now against `now + RISING_LOOKAHEAD_MS`.

## Service worker and releases

- **Version stamping is CI's job.** `.github/workflows/version-stamp.yml` runs on every push to `main` (skipping its own commits) and rewrites `VERSION` in `sw.js` and the `#version-label` fallback in `index.html` to `YYYYMMDD.NNNN` (date of the head commit, workflow run number). It commits as `github-actions[bot]` straight to `main` — the one sanctioned exception to "never push to main". Local `main` is one commit behind after every merge; pull before branching.
- The client shows the "update available · RELOAD" banner when an activating worker broadcasts a version different from the label (Decision #21).
- `sw.js`: install precaches `index.html` (required) plus the manifest, the three fonts and the four icons (other failures are swallowed and backfilled at runtime). Navigation is stale-while-revalidate, so a new `index.html` shows on the launch after the one that fetched it. `skipWaiting` + `clients.claim`; old caches are deleted on activate; an inline offline fallback page covers a cold offline launch; offline misses get a 504.
- Platform handling in `index.html`: `prefers-reduced-motion` disables transitions and animations; `contain: layout style` on cards and log rows; a `<noscript>` message; iOS standalone meta tags and safe-area insets; a 150 ms debounced resize redraw.

## History

`git log` is the full record. Milestones:

- Phases 1–5: bug fixes, smarter graph window, crosshair dots, unified scrub/time chip, PK-threshold pill visibility, clearing cards
- Phase 6: graph window anchored to the day; "taken today" = calendar day
- Phase 7: README cleanup; "in system" label → "mg eq"
- Phase 8: dose preview (ephemeral, never persisted)
- Phase 9: `isRising` uses the PK slope; clearing bar removed
- Phase 10: Bateman equal-rate limit fix (#19); fasted CR model (single phase 20 mg, ka 1.0, default); per-dose fed/fasted toggle; GitHub Issues workflow
- UX pass (#23–#29): design tokens, Space Grotesk + DM Mono, press feedback (JS touchstart for iOS), undo toast drain bar, fed/fasted toggle switch, 400 ms `.just-fired` disable, double-tap zoom prevention
- Color clarification (#33–#36): neutral dose buttons; fed toggle green; CR lavender; sublabels "5mg" / "10mg" / "20mg"
- Project hygiene (#37): workflow rules, Decision Log, `AGENTS.md` as source of truth. The PR and issue templates it added were removed on 2026-05-09.
- Papercuts and named constants (#38–#40); offline launch and SW fallback fixes (#45–#47)
- Audit pass (#48–#57): code-review cleanup, data-driven `hasFedToggle`, CSS tokens, accessibility, curve memo + binary-search tooltip, SW hardening + update banner, reduced motion / `contain` / noscript
- Stability and performance (#58–#63): render paused when hidden, time-picker focus fixes, hoisted chart listeners, CI version stamping, self-hosted fonts, compositor animations + SVG rebuild skip + draining bars, scrub tooltip UX
- Data safety + identity (#64): history kept forever, JSON export, M-curve app icon + manifest, rotation redraw
- Symkinet MR 20 mg (#65): separate `SYMR20` type, 2×2 dose grid
- Housekeeping (#66, 2026-09-22): this file split out of `AGENTS.md`; tracked `CLAUDE.md` pointer; session files gitignored; distinct version per push
- Concerta (2026-09-22): zero-order phase kind, Concerta 72/36/18, IR/CR/Symkinet retired to history-only

## Decision Log

Judgment calls made during development. Add an entry whenever a non-obvious choice is made so future sessions do not re-litigate it. New rows carry a `(verified: …)` or `(inference — unconfirmed)` tag for their factual basis.

| # | Decision | Rationale | PR / phase |
|---|----------|-----------|------------|
| 1 | Single-file architecture: all app logic in `index.html` | Rejected frameworks and build tools. Zero build step; one file is auditable with no dependency surface. `sw.js`, `manifest.webmanifest`, `fonts/` and `icons/` ship alongside as static assets. | Phase 1 |
| 2 | CR fasted ka=1.0 | Slower than pure IR (2.0) to reflect residual bead-matrix retardation; faster than fed delayed phase (0.7). Consistent with Haessler et al. 2008 "steady absorption / single Tmax" fasted profile. Population-average estimate, not a measured value. | #21 |
| 3 | `toggleFed()` updates the DOM directly and does not call `render()` | `render()` re-creates the pill card and kills the `var(--t-toggle)` slide transition on the toggle thumb. `toggleFed` toggles classes on `.fed-track`/`.fed-lbl`, syncs `aria-checked`, rewrites only the card's `.pill-body` via `updatePillCardBody()`, then calls `updateStats()` and `renderChart()`. The toggle button itself is never replaced, so the transition plays. | #27 |
| 4 | Graph window left edge anchors to today's first dose, not a rolling window | A rolling window scrolls during the day and makes morning doses hard to read. The left edge is the earliest dose since local midnight minus 45 min, but never later than now − 60 min; see "Rendering" above for the full rule. | Phase 6, #40 |
| 5 | "taken today" stat uses calendar day; PK curve uses rolling 24h | Two different questions: what have I taken since waking (calendar) vs current plasma estimate (rolling decay). Conflating them would make one of the two wrong. | Phase 6 |
| 6 | CR color `--cr: #9d7fd4` (lavender/violet) | Earlier warm orange (`#f5a050`, then `#b08860`) was too close to IR warm-brown at various screen brightness levels. Lavender gives clear hue separation from IR blue (`#5bb8f5`) and fed-toggle green (`--green`). `MEDS.CR.color` must always match `--cr`. | #33–34 |
| 7 | Dose buttons neutral (`--muted` border), no type color | Color lives on the graph and pill cards (output). Buttons are input; type-coloring them created visual double-encoding and made interaction/state colors (pressed, disabled) ambiguous. | #33–36 |
| 8 | `touch-action: manipulation` on `html, body` | Blocks iOS double-tap zoom globally without disabling pinch zoom or accessibility scaling. Alternative (`user-scalable=no` in viewport meta) disables all scaling. | #29 |
| 9 | `AGENTS.md` as universal source of truth; `CLAUDE.md` as thin pointer | AGENTS.md is the Linux Foundation AAIF universal standard (Dec 2025), backed by Anthropic, OpenAI, Google, Microsoft, and all major AI coding tools. Every agent reads the same authoritative context. Amended by Decision #31: the Decision Log and architecture now live in this file, and `CLAUDE.md` is tracked again as an `@AGENTS.md` import. | #37 |
| 10 | `CLEARING_THRESHOLD = 0.1` — 10% of totalMg is the "cleared" criterion | Pills remain visible and show a "clearing" state until plasma concentration drops below 10% of the total dose. 10% is a judgment call balancing meaningful residual effect vs. excessive tail visibility. Used in `pillIsVisible` and `pillCardBodyHTML`; must be kept as a single named constant so both sites stay in sync. | #40 |
| 11 | `RISING_LOOKAHEAD_MS = 5 * 60000` — 5-minute forward window for rising classification | The "↑ rising" label compares concentration now vs. 5 minutes ahead. 5 minutes is a judgment call: short enough to respond quickly after a dose, long enough to avoid toggling on noise near the peak plateau. Used in `updateStats` and `logRowHTML`; must be kept as a single named constant. | #40 |
| 12 | `hasFedToggle` data-driven from `MEDS[type].fastedPhases`, not hardcoded `'CR'` | Adding a new med type with a fasted profile no longer requires a parallel update to `hasFedToggle` or to `phasesFor`. The MEDS table is the single source of truth for which types have a fed mode; the helpers derive from it. | #50 |
| 13 | `UNDO_DURATION_MS` single source for both `setTimeout` and CSS `@keyframes drain` duration | The toast lifetime previously lived in two places (`animation: drain 8s` in CSS and `setTimeout(..., 8000)` in JS). Drift between them would have made the drain bar end visibly before/after the toast itself. JS constant feeds CSS via `style.setProperty('--undo-duration', ...)` at init. | #50 |
| 14 | Press-feedback timing stored in a `WeakMap`, not on the DOM element | `el._pressAt = Date.now()` pollutes the element's own-property namespace and ties the lifetime of the timing data to the element reference. WeakMap entries are GC'd automatically when the element is removed; the IIFE scope keeps the map private. | #50 |
| 15 | Animation timing tokens (`--t-base/.15s`, `--t-press/.08s`, `--t-toggle/.2s`, `--t-fill/.3s`) | Every `transition:` declaration uses a token instead of a bare duration. Tokens make the interaction-feel a single-edit-point change. CSS custom properties work inside the `transition` shorthand in Safari 15.4+ (well within the iPhone target). | #51 |
| 16 | Color tint tokens (`--ir-tint`, `--ir-tint-sim`, `--green-tint`) and `MEDS.IR.color` for SVG curve | Inline `rgba()` literals scattered across CSS rules made tweaking opacity inconsistent. The SVG concentration curve previously had six `"#5bb8f5"` string literals; now derives from the same `MEDS.IR.color` that feeds pill cards and fills. CSS doesn't (yet) allow `var()` inside `rgba()` without `color-mix`, hence the explicit tint tokens. | #51 |
| 17 | Fed toggle is a `<button role="switch" aria-checked>`, not a `<div onclick>` | Native button semantics give keyboard operability (Space/Enter), correct screen-reader announcement ("fasted/food switch, checked/unchecked"), and `:focus-visible` styling for free. `toggleFed()` syncs `aria-checked` in its direct DOM update path so the attribute stays current without a full re-render (preserves Decision #3's slide-transition). | #52 |
| 18 | `buildCurve` single-slot memoization keyed on pill fingerprint + window | The 30-second render tick is the hot path; the same call repeats with identical inputs until a real mutation. `renderChart` calls `buildCurve` once with confirmed + preview pills and derives the confirmed-only total from the same data, so one slot covers every repeat. The key encodes `id:type:takenAt:fed` for every pill, so any pill or window change invalidates it. A second layer, `_lastSvgKey`, skips the SVG rebuild entirely when curve, width and now-pixel are unchanged (#62). | #53, #62 |
| 19 | `closestPoint` binary search replaces `data.reduce` in the tooltip | `chartData` is sorted ascending by `ts` (built left-to-right in `buildCurve`). Binary search is ~7 comparisons for the typical 144-point window. The boundary check (`lo > 0 && abs(lo-1) <= abs(lo)`) correctly resolves which of the two adjacent sample points is nearer to the scrub cursor. Lives in the module-level `_showTooltip` (#58). | #53 |
| 20 | SW non-navigation fetch caches successful responses on first use, fire-and-forget | Originally for Google Fonts, whose URLs vary by User-Agent. Since Decision #22 (PR #61) the app makes no cross-origin requests; the runtime cache now backfills any precache entry whose install fetch failed. `cache.put` is unawaited so the network response isn't delayed by the cache write. | #54 |
| 21 | Update banner triggers on version-string mismatch in client, not on a separate SW message type | The `VERSION` postMessage already fires on every `activate`. Comparing the previous `#version-label` text to the new version detects updates without adding a new message type. `prev && prev !== new` correctly avoids a false positive on the first activate (when `prev` is the static fallback that matches the new VERSION). | #54 |
| 22 | Self-hosted WOFF2 fonts; eliminated Google Fonts render-blocking link | The Google Fonts `<link rel="stylesheet">` was render-blocking on iOS Home Screen launches, and every new version cleared the caches, forcing a re-fetch from `fonts.googleapis.com` (two cross-origin TLS hops). Fix: three WOFF2 latin-subset files in `fonts/`, pre-cached during SW install. Space Grotesk 600 and 700 share one file (variable font). Relative `url('fonts/...')` paths in `@font-face` src (no leading slash) because the app is served at the `/medikinetics/` subpath on GitHub Pages. | #61 |
| 23 | Dose history kept forever; rendering filters, storage never prunes | The 48h cutoff in `loadPills` silently destroyed medication history. All display/math paths already filter through `last24h()`, so keeping everything changes no behavior. Cost ~60 bytes/dose (~25 KB/year) against a ~5 MB localStorage budget. `loadPills` keeps an entry only if `takenAt` is a positive number and `type` is a string that is a key in `MEDS` — so a med type must never leave `MEDS`, or its history is dropped on the next load and save. | #64 |
| 24 | Export via Web Share API file with blob-download fallback | Target is iPhone Safari standalone PWA: `navigator.share({files})` opens the share sheet (AirDrop / Save to Files), which is the native path; `<a download>` is unreliable in standalone mode. Exported doses are enriched (mg, ISO time) so the file is readable by humans and future tools without the MEDS table. `AbortError` (user closed the sheet) is not a fallback trigger. | #64 |
| 25 | App icon: M-shaped curve — two stacked Bateman humps (IR ka=2.0, doses ~3.2h apart), generated PNGs from `icons/icon.svg` | The icon is the app's own math: two stacked doses draw a natural "M" (Medikinetics) and depict dose stacking, the core reason the app exists. apple-touch-icon must be PNG (iOS ignores SVG), so PNGs are rendered from the SVG source via macOS qlmanage/sips — no build dependency added. SVG doubles as favicon. | #64 |
| 26 | Symkinet MR 20mg is a distinct `SYMR20` type with 10mg at 0h (ka=2.0) + 10mg at +4h (ka=0.7), and no food toggle | The official Polish ChPL specifies approximately 50% immediate release, the remainder after about 4h, and no difference in overall bioavailability with or without food. The shared `KE=0.347` matches its reported ~2h half-life. The delayed `ka=0.7` reuses the app's existing biphasic approximation and is not a measured Symkinet constant. Keeping a separate type avoids incorrectly inheriting Medikinet CR's fasted single-phase model. | #65 |
| 27 | Symkinet uses muted saffron `--sym: #d4ad68`; four dose buttons use a 2×2 grid | Color remains output-only and separates Symkinet from IR blue, CR lavender, food-state green, and the red now marker. Two columns preserve equal, generous iPhone tap targets instead of leaving a fourth button stranded on a second row. Layout superseded by Decision #36. | #65 |
| 28 | `visibilitychange` manages the render interval — cleared on hide; immediate re-render and restart on foreground | iOS throttles background `setInterval` but does not stop it — it burns battery and fires irregularly. Clearing on `document.hidden` and re-rendering on show gives a fresh state on every foreground with zero background CPU. (verified: end of `<script>`, `_renderInterval`) | #58 |
| 29 | `render()` skips rebuilding `#offset-row` while the time input is in use | `innerHTML` replacement destroys the focused element and dismisses the native time picker mid-entry. The guard checks both `document.activeElement?.id === 'manual-time-input'` (PR #58) and the `_timeInputActive` flag (PR #59). The flag exists because `change` fires on intermediate picks (hour before minute), and because on some platforms (Android Chrome) the picker dialog takes focus away from the input, so the focus check alone fails. The flag is set on focusin, click and `change` — the `change` handler sets it right before its own `render()`, so that render never rebuilds the row. It clears 300 ms after `focusout` (outlasting the picker's close animation) or in `setOffset`; the row is rebuilt on the next render after that. Do not clear the flag in the `change` handler. (verified: `render()` offset-chip block, time-input listeners, commit 170122c) | #58, #59 |
| 30 | Chart tooltip handlers attached once to the stable `#chart-container`, not to the recreated `#chart-hit` each render | Every `renderChart` call replaced the SVG `innerHTML` (including `#chart-hit`) and allocated five new closures over the chart data. Module-level `_showTooltip`, `_hideTooltip` and `_attachChartHandlers` read the shared `_chart` object, and `_chartScrubbing` gates `touchmove` `preventDefault`. No per-tick allocation, same tooltip behavior. (verified: `_attachChartHandlers`) | #58 |
| 31 | `AGENTS.md` is the lean shared entry point; architecture, history and this Decision Log live in `ARCHITECTURE.md`; `CLAUDE.md` is tracked as an `@AGENTS.md` import | Claude imports `AGENTS.md` into every session and Codex loads it automatically, so its size is paid on every turn by every agent. 64% of its 16 KB was this log, which is only needed when touching what a row names. Follows the `~/Claude/code` convention (architecture in `ARCHITECTURE.md`, read on demand). `AGENTS.md` stays the single truth file for rules (#9), and its Workflow step 1 sends agents here. (verified: byte count of the pre-split `AGENTS.md`) | housekeeping |
| 32 | CI version suffix is the workflow run number (`YYYYMMDD.NNNN`), not a fixed `.0001` | With `.0001`, a second merge on the same date produced a byte-identical `sw.js`: no worker update, no cache purge, no update banner. `github.run_number` is unique per run, and zero-padding to four digits keeps the existing `sed` patterns matching. The client compares versions for inequality only (Decision #21), so the format change needs no client change. (verified: `version-stamp.yml`, `sw.js` message handler) | housekeeping |
| 33 | Zero-order (constant-rate) release is a second phase kind, selected by `windowHours` | Concerta's osmotic core releases over ~10–12h. The best first-order-only fit (22% bolus + 78% at +3.25h, ka 0.23) matches Tmax but dips to ~55% of the early shoulder at 3h, which no published curve shows; zero-order windows reproduce the flat 1–4h shoulder. Integrating the existing Bateman response over the window keeps one PK model, one `KE` and one `NORM`, so doses of any kind still add up on one scale. (verified: research writeup + node check, 2026-09-22) | Concerta |
| 34 | Concerta phase set: 22% bolus; 20% over 1–4h, 36% over 4–7h, 22% over 7–12h; ka 2.0; shared `KE` 0.347; fractions sum to 1.0; no food mode | Shape follows the FDA review's in-vivo release estimate and the label (see PK model). `KE` stays shared although the Concerta label gives t½ 3.5h: the model's tail runs ~1h short of the label's, which costs little, and a per-med `KE` would change how Concerta stacks with IR. Relative bioavailability vs IR is 91 ± 10%, inside the error of plot-read fractions and irrelevant to a relative "mg eq", so no scaling. The label finds no clinically meaningful food effect, so no `fastedPhases`. (inference — unconfirmed: fraction values read off a scanned plot, accurate to a few percent) | Concerta |
| 35 | Meds no longer taken get `retired: true` instead of being deleted | `loadPills` drops stored doses whose type is not a `MEDS` key and the next save makes it permanent, so deleting IR/CR/Symkinet would erase the owner's history. `retired` only removes the dose button; cards, log, chart, export and undo still resolve the type. `MEDS.IR.color` stays in use as the total-curve color. | Concerta |
| 36 | Three Concerta buttons (72 / 36 / 18) in the two-column grid with the first spanning the row; Concerta color rose `#e58fb8` (`--con`) | 72mg (2 × 36) is the daily dose, one tap; 36 and 18 cover the prescribed step-ups to 90 / 108mg as a second tap, since the model is linear and 2×18 is bioequivalent to 1×36. An odd count in two columns would strand the last button (the concern behind Decision #27), so the first button spans the row. Rose is a hue no other on-screen color uses (IR blue, CR lavender, Symkinet saffron, green status, red now marker, grey-blue rising). Supersedes the 2×2 layout in #27. (Look is the owner's call; proposed, not judged.) | Concerta |
